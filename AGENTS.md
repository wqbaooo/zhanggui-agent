# 掌柜Agent 项目协作契约

## 当前产品口径

掌柜Agent 当前只服务一个真实对象：

```text
新余恒太城五楼大口章鱼烧
```

它不是加盟顾问、不是通用餐饮 SaaS、不是外卖平台、不是 POS 替代品。它是一个以利润增长为目标的 **AI 单店经营 Agent**：

```text
说话 / 拍照 / 截图 / 上传资料
  -> Agent 识别来源和字段
  -> 用户确认
  -> 写入这家店的经营档案
  -> 自动分析利润、库存、人工、渠道、SOP 和风险
  -> 生成老板能执行的下一步动作
```

最高目标：

```text
在食品安全、商场规则、员工可承受、顾客体验不崩的前提下，持续提高这家店的真实净利润。
```

默认项目 ID 是：

```text
xinyu-hengtai-dakou
```

前端、后端、播种脚本、测试和文档如果需要访问默认门店，都应使用这个 ID。不要再新增或恢复历史演示项目 ID 作为产品默认对象。

## 当前工程状态

- 后端：FastAPI，入口在 `server/main.py`。
- 前端：Next.js，入口在 `web/`，默认端口 `3005`。
- 真实门店数据目录：`project_data/xinyu-hengtai-dakou/`。
- 核心产品契约：`README.md`、`PRODUCT.md`、`DESIGN.md`。
- Agent/门店静态结构：`web/src/data/agent-store-os.ts`。
- 前端默认 API 常量：`web/src/lib/api.ts`。
- 数据播种脚本：`scripts/seed_store.py`。

## 不要恢复的历史口径

不要把产品重新描述成以下任何一种：

- 加盟前决策工具
- 开店准备百科
- 选址/加盟主导航产品
- 通用餐饮创业顾问
- 静态 dashboard 或报表堆叠

旧的加盟、选址、开店前资料可以作为历史知识或风险材料保留，但不能再作为主产品定位和默认导航。

## 产品结构

前端分四个大区：

1. **Agent 工作台**
   - 今日状态
   - 对话/拍照/截图录入
   - 待确认草稿
   - 主动预警
   - 今日三件事

2. **经营驾驶舱**
   - 营业走势
   - 利润与保本
   - 渠道外卖
   - 商品走势
   - 进货库存
   - 水电耗材
   - 天气商圈

3. **SOP 作业系统**
   - 产品 SOP
   - 备料 SOP
   - 开店/营业中/打烊 SOP
   - 卫生检查
   - 员工训练

4. **店铺档案**
   - 客如云截图
   - 美团/淘宝闪购/抖音后台截图
   - 进货单
   - 水电单
   - 总部资料
   - 员工档案
   - 合同/规则
   - 周报月报

## 后端业务优先级

下一阶段优先深挖后端业务逻辑，顺序为：

1. ~~SKU 预测精准度：断货日、建议采购量、采购金额、风险等级。~~ ✅ 已完成
2. 工资核算：全职/月薪、兼职/时薪、加班、老板本人守店、日均人工摊销。
3. 周报自动生成：指定周期、异常检测、库存/人工/渠道发现、行动项写回。

## 协作规则（强制）

每次对话必须遵守三步节奏：

### 1. 执行前 → 先说方案，等确认

任何代码改动前，先说清楚：
- 要改什么、为什么
- 影响哪些文件
- 具体怎么做

等待用户回复"确认"或"执行"后再动手。不允许跳过此步直接改代码。

### 2. 收到回复 → 先复述理解

用户回复后，一句话复述你理解的意思，确保和用户想的一致。不一致时先澄清再继续。

### 3. 改动完成 → 简短汇报 + 前端链接

每次迭代改动完成后：
- 3-5 句话说明改了什么
- 给出前端页面链接让用户查看效果
- 说明跑了哪些验证

汇报格式：
```
改动：xxx
改了：xxx
验证：xxx
前端：http://127.0.0.1:3005/xxx
```

### 例外

单文件一行配置/文案修正，可跳过第 1 步确认，直接改完汇报。

## 工作流

1. 先确认产品契约、默认项目 ID 和数据目录。
2. 再改最小闭环，不顺手恢复旧的加盟/选址主线。
3. 改后至少跑一个后端窄测试或前端构建。
4. 最终回复必须说明实际跑过的验证；没跑的验证要明确。

推荐命令：

```bash
pytest tests/test_project_api.py -q
cd web && npm run build
cd web && npm run lint
```

已知限制：

- 全量 `pytest -q` 仍可能被旧 `core.session` / `core.llm_client` 测试链路阻断。
- 前端 lint 可能仍有新页面的 React hooks / unused warnings，需要单独清理。
